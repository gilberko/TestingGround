package com.example.southkoreatravel.screens

import androidx.compose.runtime.Composable

@Composable
fun SeoulScreen(onBack: () -> Unit) {
    ScreenScaffold(title = "Seoul", onBack = onBack) {
    }
}
